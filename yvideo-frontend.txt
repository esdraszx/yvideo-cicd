pipeline {
  agent any
  stages {
    stage('Checkout Scm') {
      steps {
        git(credentialsId: 'github-new', url: 'https://github.com/esdraszx/yvideo-client.git')
      }
    }

    stage('Shell script 0') {
      steps {
        sh '''#The current directory is jenkins home. We need to do everything in /srv/y-video-back-end/y-video-back-end
#this job will be triggered 
#	a change to the front-end repository


cd /srv/y-video-back-end/y-video-back-end

cd yvideo-client

git pull origin develop

cd ../

./build-front-end.sh

# After this the back-end triggers and will deploy the changes'''
      }
    }

  }
}
